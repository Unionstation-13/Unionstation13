#define ADMIN    1 // Gives default sudo access meaning no cracker needed
#define HOMEMADE   2 // Doesn't have default sudo access; needs to escalate w/ password cracker(just takes time)
#define TRAITOR   4 // Gives malware compilation ability
